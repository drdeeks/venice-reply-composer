#!/usr/bin/env node

/**
 * TypeScript configuration for Uniswap v4 Hook Revenue Share project
 */

module.exports = {
  compilerOptions: {
    // Target modern environments
    target: "ES2020",
    module: "commonjs",
    
    // Output configuration
    outDir: "./dist",
    rootDir: "./src",
    
    // Path resolution
    baseUrl: ".",
    paths: {
      "@/*": ["src/*"],
      "@contracts/*": ["contracts/*"],
      "@interfaces/*": ["contracts/interfaces/*"],
      "@libraries/*": ["contracts/libraries/*"],
      "@mocks/*": ["contracts/mocks/*"],
      "@scripts/*": ["scripts/*"],
      "@test/*": ["test/*"],
    },
    
    // Module resolution
    moduleResolution: "node",
    allowImportingTsExtensions: true,
    
    // Type checking
    strict: true,
    noImplicitAny: true,
    noImplicitReturns: true,
    noFallthroughCasesInSwitch: true,
    
    // Additional checks
    noUncheckedIndexedAccess: true,
    exactOptionalPropertyTypes: true,
    
    // Experimental features
    esModuleInterop: true,
    allowSyntheticDefaultImports: true,
    
    // Source maps
    sourceMap: true,
    inlineSources: true,
    
    // Target environments
    lib: ["ES2020", "DOM", "DOM.Iterable"],
    
    // Skip type checking for external modules
    skipLibCheck: true,
    
    // Enable experimental decorators (if needed)
    experimentalDecorators: false,
    emitDecoratorMetadata: false,
  },
  
  // Include patterns
  include: [
    "src/**/*",
    "scripts/**/*",
    "test/**/*",
    "package.json",
  ],
  
  // Exclude patterns
  exclude: [
    "node_modules",
    "dist",
    "build",
    "coverage",
    " foundry-report",
    "contracts/mocks/**/*",
    "contracts/interfaces/**/*",
    "docs",
    "*.log",
  ],
  
  // Type roots for custom types
  typeRoots: [
    "./node_modules/@types",
    "./src/types",
  ],
  
  // Additional compiler options
  compilerOptions: {
    // Enable incremental compilation
    incremental: true,
    
    // Preserve symlinks
    preserveSymlinks: true,
    
    // Enable watch mode for development
    watchOptions: {
      watchFile: "fixedPolling",
      watchDirectory: "useFsEvents",
      fallbackPolling: "fixedPolling",
    },
    
    // Bundle output (if needed)
    declaration: true,
    declarationMap: true,
    
    // Performance optimizations
    isolatedModules: true,
    noEmitOnError: true,
  },
};